services:
  portal:
    build: .
    container_name: t3mp3st-portal
    network_mode: host
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PORT=8888
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-prod}
